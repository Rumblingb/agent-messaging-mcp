runtime: "container"
startCommand:
  type: "stdio"
  command: "node"
  args: ["index.js"]
description: "Async agent-to-agent messaging over MCP. Messages, proposals, threaded conversations. The inbox layer for multi-agent systems."
